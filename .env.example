# Supabase project settings — Project Settings > API Keys in the Supabase dashboard.
# Current key format: publishable (client-safe) and secret (server-only),
# replacing the legacy anon/service_role naming.
NEXT_PUBLIC_SUPABASE_URL="https://<project-ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
# Server-only — never prefix with NEXT_PUBLIC_, never sent to the browser.
SUPABASE_SECRET_KEY="sb_secret_..."

# PostgreSQL — pooled connection (Supabase "Transaction" pooler, port 6543).
# Project Settings > Database > Connection string in the Supabase dashboard.
DATABASE_URL="postgresql://postgres.xxxx:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"

# PostgreSQL — direct/unpooled connection (port 5432), used only for `prisma migrate`
DIRECT_URL="postgresql://postgres.xxxx:password@aws-0-region.pooler.supabase.com:5432/postgres"

# Anthropic API key, used server-side only for AI meal recognition / insights (Phase 2)
ANTHROPIC_API_KEY=""

# USDA FoodData Central API key (free, register at fdc.nal.usda.gov)
USDA_API_KEY=""

# Google Maps Platform API key, used server-side only for restaurant
# search (Places API). Get one at console.cloud.google.com -- enable
# "Places API" (legacy) for the project, and restrict the key to that
# API specifically. Has a free monthly usage tier; beyond that it's a
# paid, metered API, so treat usage/quotas as a real cost consideration,
# not just a feature flag.
GOOGLE_MAPS_API_KEY=""

# Web Push (reminders). Generate a pair with:
#   npx web-push generate-vapid-keys
# Public key is exposed to the browser (needed for pushManager.subscribe);
# private key is server-only and must never be committed or exposed client-side.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
# mailto: or https: URL identifying who's sending the push, per the Web Push spec.
VAPID_SUBJECT="https://caltrax.kavauralabs.com"

# Shared secret for the /api/cron/reminders endpoint. Vercel Hobby's cron
# scheduler is limited to once/day, so this project calls that route from a
# free external scheduler (e.g. cron-job.org) every 5-15 min instead — set
# the same value here and as that scheduler's Authorization: Bearer header.
# Generate one with: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
CRON_SECRET=""

# Fitbit (Phase 4 wearable sync — steps + active calories).
# Register a "Personal" app at dev.fitbit.com/apps/new — approved instantly,
# no review wait. OAuth 2.0 Application Type: Server. Set the Redirect URI
# to https://caltrax.kavauralabs.com/api/wearables/FITBIT/callback (and
# http://localhost:3000/api/wearables/FITBIT/callback for local dev — Fitbit
# allows multiple redirect URIs on one app).
FITBIT_CLIENT_ID=""
FITBIT_CLIENT_SECRET=""

# Withings (Phase 4 wearable sync — smart-scale weigh-ins).
# Register an app at developer.withings.com — also self-serve, instant
# approval. Set the callback URL to
# https://caltrax.kavauralabs.com/api/wearables/WITHINGS/callback (and the
# localhost equivalent for local dev).
WITHINGS_CLIENT_ID=""
WITHINGS_CLIENT_SECRET=""

# Oura (Phase 4 wearable sync — steps + active calories from an Oura Ring).
# Register an OAuth app at cloud.ouraring.com/oauth/applications —
# self-serve, instant. Redirect URI:
# https://caltrax.kavauralabs.com/api/wearables/OURA/callback (+ localhost
# equivalent for local dev).
OURA_CLIENT_ID=""
OURA_CLIENT_SECRET=""

# Whoop (Phase 4 wearable sync — strain-based calorie burn + resting HR).
# Register an app at developer.whoop.com — the OAuth app itself is
# self-serve, but Whoop has historically gated wider production/
# user-facing access behind their own separate review on top of that, so
# treat this one as slower to fully go live than the other three.
# Redirect URI: https://caltrax.kavauralabs.com/api/wearables/WHOOP/callback
# (+ localhost equivalent for local dev).
WHOOP_CLIENT_ID=""
WHOOP_CLIENT_SECRET=""
